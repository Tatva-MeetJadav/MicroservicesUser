@using MicroservicesUser.Models.ViewModels.History

@model EmailVerificationListHistoryVM
<div class="table-email-verification table-responsive mt-3 table-shadow-custom">
    <table class="table table table-bordered table-striped m-0">
        <thead class="table-primary border-black">
            <th scope="col" class="text-white">Email</th>
            <th scope="col" class="text-white">Valid</th>
            <th scope="col" class="text-white">VerifiedAt</th>
            <th scope="col" class="text-white">Deliverability</th>
            <th scope="col" class="text-white">OverAllScore</th>
            <th scope="col" class="text-white">View Detail</th>
        </thead>
        <tbody>
            @if (Model.EmailVerificationHistoryListVM != null)
            {
                @foreach (var item in Model.EmailVerificationHistoryListVM)
                {
                    <tr>
                        <td>@item.Email</td>
                        <td>@item.Valid</td>
                        <td>@item.VerifiedAt</td>
                        <td>@item.Deliverability</td>
                        <td>@item.OverAllScore</td>
                        <td><i class="bi bi-eye"></i></td>
                    </tr>
                }
            }

        </tbody>
    </table>
</div>

<div class="d-flex rounded-1 justify-content-center my-2 p-2 bg-white @(Model.TotalItems == 1 ? "d-none" : "")">
    <div class="d-flex flex-sm-row flex-column justify-content-sm-start gap-2 align-items-center">
        <div class="d-flex align-items-center gap-2">
            <div>
                <span>Items Per Page</span>
            </div>
            <div>
                <select class="form-select items-per-page form-select-sm"
                    data-pagination-info="@Model.EmailVerificationHistoryListVM?.Count()">
                    <option value="5" selected="@(Model.PageSize == 5)">5</option>
                    <option value="10" selected="@(Model.PageSize == 10)">10</option>
                    <option value="15" selected="@(Model.PageSize == 15)">15</option>
                    <option value="20" selected="@(Model.PageSize == 20)">20</option>
                </select>
            </div>
        </div>
        <div class="d-flex gap-2 align-items-center">
            <span class="me-2 pagination-info" data-current-page="@Model.CurrentPage">
                Showing @((Model.CurrentPage - 1) * Model.PageSize + 1)-@((Model.CurrentPage - 1) * Model.PageSize
                                + Model.EmailVerificationHistoryListVM?.Count()) of @Model.TotalItems
            </span>
            <button class="btn prev-page btn-primary @(Model.CurrentPage == 1 ? "disabled" : "")">
                <i class="bi bi-chevron-left"></i>
                <span>Previous</span>
            </button>
            <button
                class="btn next-page btn-secondary @(Model.CurrentPage* Model.PageSize >= Model.TotalItems ? "disabled" : "")">
                <span>Next</span>
                <i class="bi bi-chevron-right"></i>
            </button>
        </div>
    </div>
</div>